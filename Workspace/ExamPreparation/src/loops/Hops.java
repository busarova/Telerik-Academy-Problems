package loops;

import java.util.ArrayList;
import java.util.Scanner;

public class Hops {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);

		ArrayList<String> input = new ArrayList<>();

		while (true) {
			input.add(sc.nextLine());
		}

	}

}
/*
 * The input will be an array of strings The first element will be the field
 * with carrots - numbers separated with “, ” (comma and space). The second
 * element will be M – the number of directions to try. The next M elements of
 * the array will be numbers, separated with “, “(comma and space), representing
 * the directions themselves.
 */
